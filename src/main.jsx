import React from 'react';
import ReactDOM from 'react-dom/client';
import TruuGoApp from './App.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <TruuGoApp />
  </React.StrictMode>
);
